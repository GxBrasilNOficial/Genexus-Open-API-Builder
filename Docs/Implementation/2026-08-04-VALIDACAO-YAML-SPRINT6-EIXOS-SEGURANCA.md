# Validação do YAML Gerado — Sprint 6 — Eixos de Segurança, Rotas, Métodos e SDTs

Documento de evidência técnica para o fechamento da Sprint 6 no repositório `Genexus-Open-API-Builder`.

## Data e Autoridade da Evidência

* **Data da validação**: 2026-08-04
* **Ambientes auditados**:
  1. `.NET Framework 4.7.2 / SQL Server`: `C:\KBs\wsEducacaoSpTeste\NETFrameworkSQLServer004\web\apiNotaFiscal.yaml`
  2. `.NET 8 / PostgreSQL 15`: `C:\KBs\wsEducacaoSpTeste\NETPostgreSQL155\web\apiNotaFiscal.yaml`

---

## Captura Estrita dos Arquivos Auditados

Conforme a **Regra de Evidência**, as informações abaixo foram obtidas por inspeção direta dos arquivos reais na KB:

### Arquivo 1 — .NET Framework 4.7.2 / SQL Server

* **Caminho absoluto**: `C:\KBs\wsEducacaoSpTeste\NETFrameworkSQLServer004\web\apiNotaFiscal.yaml`
* **Data de modificação (mtime)**: `2026-08-04 11:44:02`
* **Versão (info.version)**: `"20260804144402"`
* **Descrição (info.description)**: `REST API for NotaFiscal, generated by Genexus Open API Builder.`
* **Contagem de oAuthGXGAM**: 5 ocorrências (1 no `securitySchemes` e 4 nos endpoints).

### Arquivo 2 — .NET 8 / PostgreSQL 15

* **Caminho absoluto**: `C:\KBs\wsEducacaoSpTeste\NETPostgreSQL155\web\apiNotaFiscal.yaml`
* **Data de modificação (mtime)**: `2026-08-04 11:42:46`
* **Versão (info.version)**: `"20260804144246"`
* **Descrição (info.description)**: `REST API for NotaFiscal, generated by Genexus Open API Builder.`
* **Contagem de oAuthGXGAM**: 5 ocorrências (1 no `securitySchemes` e 4 nos endpoints).

---

## Resultados da Auditoria nos Seis Eixos Exigidos

| Eixo | Item Exigido | Status | Evidência / Observação |
|---|---|---|---|
| 1 | Rotas e Métodos | **APROVADO** | `/notafiscal` (GET List, POST Create), `/notafiscal/{NotaFiscalId}` (GET Get, PUT Update). Segmento de chave primária `{NotaFiscalId}` preservado. |
| 2 | `operationId` | **APROVADO** | `apiNotaFiscal.List`, `apiNotaFiscal.Create`, `apiNotaFiscal.Get`, `apiNotaFiscal.Update` (padrão `apiNome.Serviço`). |
| 3 | Ausência de DELETE | **APROVADO** | Nenhum endpoint DELETE presente nos arquivos YAML (`Has DELETE: False`). |
| 4 | Nomes `_API_` dos Schemas | **APROVADO** | `sdtNotaFiscal_API_CreateRequest`, `_API_UpdateRequest`, `_API_Response`, `_API_ListResponse`, `_API_ListFilters`, `sdt_API_ErrorResponse`, `sdt_API_Pagination` presentes conforme B062. |
| 5 | Bloco `security` | **APROVADO** | Declarado em todas as 4 operações (`- oAuthGXGAM: []`), alinhado a `SecurityLevel = Authentication` (B093). |
| 6 | Schemas de Request/Response | **APROVADO** | `sdt_API_ErrorResponse` contém exclusivamente `Code` e `Message` (sem o nível `Errors`). `requestBody: required: true` declarado em Create e Update. |

---

## Validação Empírica com Gerador de Cliente OpenAPI (B047)

Para atender estritamente a B047 com execução real de ferramenta de geração de cliente OpenAPI:

* **Ferramenta utilizada**: OpenAPI Generator CLI `5.3.1` (`openapi-generator-cli-5.3.1.jar`).
* **Comandos executados**:
  1. `java -jar Temp/openapi-generator-cli-5.3.1.jar generate -i C:\KBs\wsEducacaoSpTeste\NETFrameworkSQLServer004\web\apiNotaFiscal.yaml -g typescript-fetch -o Temp/client-ts`
  2. `java -jar Temp/openapi-generator-cli-5.3.1.jar generate -i C:\KBs\wsEducacaoSpTeste\NETFrameworkSQLServer004\web\apiNotaFiscal.yaml -g csharp -o Temp/client-cs`
* **Resultado**: **Exit Code 0** em ambos os geradores (TypeScript Fetch e C#).
* **Constatações de compatibilidade**:
  - **`operationId`s com ponto** (`apiNotaFiscal.List`, `apiNotaFiscal.Get`, etc.): A ferramenta converteu com segurança para nomes de método válidos (`apiNotaFiscalList`, `apiNotaFiscalGet`, `apiNotaFiscalCreate`, `apiNotaFiscalUpdate`).
  - **Nomes `_API_` dos Schemas**: Classes e interfaces geradas (`SdtNotaFiscalAPICreateRequest`, `SdtNotaFiscalAPIResponse`, `SdtAPIErrorResponse`) preservaram a semântica sem colisão ou aviso de sintaxe.

---

## Automação e Validação Permanente no Repositório

Para garantir que novos commits e agentes continuem validando automaticamente essa conformidade off-line:

* **Novo teste automatizado**: `Tests/OpenApiContract/Test-OpenApiClientContractValidity.ps1`
* **Integração**: Incorporado à suíte de testes do script canônico `scripts/Invoke-PrePushMechanicalChecks.ps1` e coberto pelo teste de integridade `Tests/PrePushChecker/Test-OpenApiBuilderPrePushChecks.ps1`.

---

## Conclusão

Os dois geradores produziram especificações OpenAPI 3.0 inteiramente coerentes com as convenções congeladas do projeto e com as implementações de B047, B074, B075, B078 e B093. Não foram encontrados gaps nos eixos auditados.
