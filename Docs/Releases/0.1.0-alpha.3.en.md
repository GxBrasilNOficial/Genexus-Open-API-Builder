# Release 0.1.0-alpha.3

**Date:** 2026-08-18

**Workstream:** trilingual localization, Wizard recovery and robustness, and contract standardization

**Tag:** `v0.1.0-alpha.3`
**Release:** [Alpha on GitHub](https://github.com/GxBrasilNOficial/Genexus-Open-API-Builder/releases/tag/v0.1.0-alpha.3)

**Languages:** [Português do Brasil](0.1.0-alpha.3.md) · [Español](0.1.0-alpha.3.es.md) · [English](0.1.0-alpha.3.en.md)

This release delivers full trilingual localization for the extension (Portuguese, Spanish, and English), robust state recovery in the Wizard lifecycle, and error contract standardization.

## You must choose the correct DLL

Download and install only the file that matches your IDE version:

| GitHub Release asset | For | Not for |
|---|---|---|
| `GenexusOpenApiBuilder.Extension.dll` | GeneXus 18 Upgrade 14, Upgrade 15, and later U14+ releases | GeneXus 18 Upgrade 13 |
| `GenexusOpenApiBuilder.Extension-gx18u13.dll` | GeneXus 18 Upgrade 13 | GeneXus 18 U14+ |

The `-gx18u13` suffix exists to distinguish the public download. Do not rename a DLL to make it look like another line, and do not install both DLLs in the same IDE.

## What is included

- **Full trilingual localization:** automatic detection based on KB language with a centralized string catalog (`pt-BR`, `es`, `en`), translation of manifest commands, dialogs (Wizard, Synchronization, Preferences, Final Report), and Output window messages.
- **Wizard selection and filter restoration:** when reopening the Wizard on an existing API, selected services, fields, and custom filters are accurately restored from metadata, SDTs, and API Object.
- **Robust Wizard re-encountering:** support for custom API Object names via metadata, support for partial `List`/`Get`-only generation plans, tolerant `Service Source` parsing without duplicate key errors, and reading owned SDTs without metadata.
- **HTTP contract standardization:** `List` error contract aligned with `Create` and `Update` using `ErrorResponse`, `RestStatusCode=400`, and removal of `msg()`.
- **Metadata integrity (B060):** fixed date parsing to avoid false positive hash mismatches from fractional second truncation in ISO-8601 strings.
- **Trilingual public documentation:** public READMEs with content parity across Portuguese, Spanish, and English.

## Validation

- U13: the satellite build producing the asset compiled in Release mode and passed offline assembly inventory validation.
- U15: canonical Release build passed, with unit test suite covering localization, filter restoration, service source parsing, metadata integrity, and ownership rules.
- Mechanical pre-push: automated suite of 40 checks (including 31 unit tests) completed with 100% pass rate.
- Assets: manifest, entry class, `PackageCompatibility`, informational version, file name, and SHA-256 validated by the Release checker.

### Asset SHA-256

| Asset | SHA-256 |
|---|---|
| `GenexusOpenApiBuilder.Extension.dll` | `A883858E164E432918DBA8CD6754B93FE556D10838637F62B5BDE37A29BC9A5B` |
| `GenexusOpenApiBuilder.Extension-gx18u13.dll` | `6A8E0ED04282FC39EDA2BC1FD992D0E87D53FB6024946756A549C397506A18C5` |

## Installation

Follow the [public installation guide](../Public/INSTALL.md). In brief:

1. choose the DLL using the table above;
2. with the GeneXus IDE open, use **Extensions Manager → Add > Local**;
3. close the IDE completely;
4. run `genexus /install` in the corresponding installation;
5. reopen the IDE and verify the Genexus Open API Builder menus.

The GitHub Packages `.nupkg` is not the IDE installer. For public installation, use the Release DLL asset.

## Known limitations

- The `DELETE` service is not part of the MVP.
- GeneXus native OpenAPI YAML retains the limitations documented in [B088](../Implementation/2026-08-10-B088-LIMITACOES-YAML-NATIVO.md).
- On IIS + .NET Framework, the `PUT` verb requires the handler adjustment described in the [README](../../README.md#requisito-de-ambiente-put-delete-e-patch-em-iis).
- Installation without elevation and updates performed only through Add > Local over an already installed DLL remain unproven.
- This is still an Alpha, not a Beta; Sprint 9 external feedback and known limitations remain explicit.

## Changelog

See the [`0.1.0-alpha.3`](../../CHANGELOG.md#010-alpha3) section of the [CHANGELOG](../../CHANGELOG.md).
